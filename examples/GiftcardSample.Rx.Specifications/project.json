{
  "targets": {
    "format": {
      "executor": "@nx-dotnet/core:format",
      "options": {
        "fix": true
      }
    },
    "test": {
      "executor": "@nx-dotnet/core:test",
      "options": {
        "logger": "trx",
        "settings": "../coverlet.runsettings",
        "resultsDirectory": "../../dist/test-results"
      }
    }
  }
}
